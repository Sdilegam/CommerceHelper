@model IEnumerable<CommerceHelperDB.Models.SuperCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SuperCategoryInGameId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SuperCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SuperCategoryInGameId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SuperCategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SuperCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SuperCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SuperCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
